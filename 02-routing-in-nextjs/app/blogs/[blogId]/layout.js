const layout = ({ children }) => {
//   throw new Error("Error in Blog Layout");
  return (
    <div>
      <h1>Blog Layout</h1>
      {children}
    </div>
  );
};

export default layout;
